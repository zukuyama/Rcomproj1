#include "protocol.h"

void init_super(super_t * s)
{ 
  s->set[0] = F;
  s->set[1] = A;
  s->set[2] = C;
  s->set[3] = (s->set[1])^(s->set[2]);
  s->set[4] = F;

  s->ua[0] = F;
  s->ua[1] = A;
  s->ua[2] = C;
  s->ua[3] = (s->ua[1])^(s->ua[2]);
  s->ua[4] = F;
}